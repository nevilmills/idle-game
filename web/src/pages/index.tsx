import { Box } from "@chakra-ui/react";
import type { NextPage } from "next";
import { NavBar } from "../components/NavBar";

const Home: NextPage = () => {
  return (
    <Box>
      <NavBar />
    </Box>
  );
};

export default Home;
